{
  "name": "cmd-cmd-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private cmd+cmd relay for sending screenshots into Codex.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js",
    "smoke:post": "node scripts/post-sample.js"
  }
}
